import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as TagStories from './Tag.stories';
import * as TagExamples from './Tag.examples.stories';

<Meta of={TagStories} />

# Tag

Tags communicate compact categorical metadata and can optionally be removable.

## Examples

### Neutral

The default style for category and keyword labels with no status meaning.

<Canvas of={TagExamples.Neutral} meta={TagExamples} />

### Brand

Emphasizes featured or highlighted tags.

<Canvas of={TagExamples.Brand} meta={TagExamples} />

### Success

Positive status tags — approved, available, in stock.

<Canvas of={TagExamples.Success} meta={TagExamples} />

### Warning

Tags that flag attention — pending review, expiring soon.

<Canvas of={TagExamples.Warning} meta={TagExamples} />

### Danger

Negative or blocking status tags — rejected, blocked, failed.

<Canvas of={TagExamples.Danger} meta={TagExamples} />

### Dismissible

<Canvas of={TagExamples.Dismissible} meta={TagExamples} />
