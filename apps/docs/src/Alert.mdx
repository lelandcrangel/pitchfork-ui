import { Canvas, Meta } from '@storybook/addon-docs/blocks';
import * as AlertStories from './Alert.stories';
import * as AlertExamples from './Alert.examples.stories';

<Meta of={AlertStories} />

# Alert

Alert surfaces contextual feedback with semantic variants and optional dismissal.

`warning` and `danger` variants use `role="alert"` (assertive — interrupts screen readers immediately). `info` and `success` use `role="status"` (polite — announces when the reader is idle). Consumers can override via the `role` prop.

## Examples

### Info

General, non-critical context or guidance. Announced politely (`role="status"`).

<Canvas of={AlertExamples.Info} meta={AlertExamples} />

### Success

Confirms an action completed successfully. Announced politely (`role="status"`).

<Canvas of={AlertExamples.Success} meta={AlertExamples} />

### Warning

Flags something that needs attention but isn't blocking. Announced assertively
(`role="alert"`).

<Canvas of={AlertExamples.Warning} meta={AlertExamples} />

### Danger

Communicates an error or failure requiring immediate attention. Announced
assertively (`role="alert"`).

<Canvas of={AlertExamples.Danger} meta={AlertExamples} />

### Dismissible

<Canvas of={AlertExamples.Dismissible} meta={AlertExamples} />
